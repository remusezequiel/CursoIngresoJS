function mostrar()
{
    var base;
    var altura;

     prompt("ingrese base").value = base;
     prompt("ingrese altura").value = altura;
    area = (parseInt(base) * parseInt(altura)/2);

    Alert("el area es" + " " + area);


}
